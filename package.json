{
  "name": "arm",
  "version": "1.0.0",
  "private": true,
  "description": "ARM — Account & Relationship Manager",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview",
    "lint": "npm --prefix client run lint"
  },
  "keywords": ["crm", "revenue", "relationship-management", "sales-pipeline"],
  "author": "ARM Team",
  "license": "MIT"
}
